<% content_for :meta_title, "Search plants - #{DEFAULT_META["meta_product_name"]}" %>

<div class="header">
  <h1>Plants for you</h1>
</div>

<div data-controller="plant-list">
  <%= render 'plant_form' %>

  <%= render 'plant_list', plants: @plants %>
</div>
